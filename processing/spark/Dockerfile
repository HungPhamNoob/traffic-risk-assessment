# processing/spark/Dockerfile - CI/CD Placeholder (Hiếu update sau)
FROM python:3.10-slim

# Install minimal Java for Spark compatibility (optional)
RUN apt-get update && apt-get install -y \
    openjdk-11-jre-headless \
    && rm